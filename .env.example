# Hugging Face Hub token — optional. Authenticated downloads are faster
# (higher rate limits) when training pulls Unsloth / student weights.
# Create a read token at https://huggingface.co/settings/tokens
HF_TOKEN=
